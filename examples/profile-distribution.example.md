# Public Profile Distribution Example

A reusable profile package should include:

- `PROFILE.md`
- `config.public.yaml`
- `manifest.json`

It must not include credentials, memory, sessions, logs, caches, state
databases, pairing state, or runtime files.
